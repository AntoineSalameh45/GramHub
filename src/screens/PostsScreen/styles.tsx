import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  viewContainer: {
    flex: 1,
    backgroundColor: '#222',
  },
  postsContainer: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
});
export default styles;
